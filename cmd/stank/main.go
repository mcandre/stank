package main

func main() {
	// Given some path(s),
	// traverse the paths recursively.
	// For each nondirectory node,
	// warn on abnormalities for POSIXy files.
	// Print all POSIXy paths, such as for xargs to process to shell script linters.
	// Omit nonPOSIXy paths.
}
